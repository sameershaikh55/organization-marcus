import React from "react";

const FormTaglines = ({ title, desc }) => {
  return (
    <>
      <h1 className="mb-0">{title}</h1>
      <p className="f18">{desc}</p>
    </>
  );
};

export default FormTaglines;
